import 'package:flutter/material.dart';

class Electrical extends StatelessWidget {
  const Electrical({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("this is electrical home screen"),
    );
  }
}